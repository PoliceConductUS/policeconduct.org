---
import { loadReportSummaries } from "#src/lib/data/summaries.js";

export async function getStaticPaths() {
  const reports = await loadReportSummaries();
  return reports.map((report) => ({
    params: {
      category: report.state,
      slug: report.slug,
    },
    props: {
      canonicalReportPath: report.canonicalPath,
    },
  }));
}

return Astro.redirect(Astro.props.canonicalReportPath, 301);
---
