---
import SiteLayout from "#src/layouts/SiteLayout.astro";
import Breadcrumbs from "#src/components/Breadcrumbs.astro";
import { loadPersonnelSummaries } from "#src/lib/data/summaries.js";
import { loadFederalAgencySummaries } from "#src/lib/data/federal-agencies.js";
import UsaChoroplethMap from "#src/components/UsaChoroplethMap.astro";
import { US_STATE_TILES } from "#src/lib/geo/states.js";
import {
  buildItemList,
  buildWebPage,
  getSiteUrl,
} from "#src/lib/structured-data.js";

const personnelRows = await loadPersonnelSummaries();
const federalAgencies = await loadFederalAgencySummaries();
const stateStats = US_STATE_TILES.map((state) => {
  const count = personnelRows.filter(
    (person) => person.agencyState?.toUpperCase() === state.code,
  ).length;
  return {
    code: state.code,
    count,
    href: `/${state.code.toLowerCase()}/`,
  };
});
const totalPersonnel = personnelRows.length;
const statesWithPersonnel = stateStats.filter(
  (state) => state.count > 0,
).length;
const pagePath = "/personnel/";
const suggestPersonnelHref = "/personnel/new/";
const siteUrl = getSiteUrl(Astro.site);
const pageUrl = new URL(pagePath, siteUrl).toString();
const personnelItems = personnelRows.slice(0, 25).map((person, index) => ({
  "@type": "ListItem",
  position: index + 1,
  item: {
    "@type": "Person",
    name: `${person.firstName} ${person.lastName}${
      person.nameSuffix ? ` ${person.nameSuffix}` : ""
    }`,
    url: new URL(`/personnel/${person.slug}/`, siteUrl).toString(),
    ...(person.agencyName
      ? {
          worksFor: {
            "@type": "GovernmentOrganization",
            name: person.agencyName,
          },
        }
      : {}),
  },
}));
const structuredData = buildWebPage({
  siteUrl,
  pageUrl,
  name: "Personnel",
  description: "Personnel profiles currently listed on PoliceConduct.org.",
  type: "CollectionPage",
});
const personnelItemList = buildItemList(personnelItems);
if (personnelItemList) {
  structuredData.mainEntity = personnelItemList;
}
---

<SiteLayout
  title="Personnel"
  description="Personnel profiles currently listed on PoliceConduct.org."
  pagePath={pagePath}
  structuredData={structuredData}
>
  <main>
    <section class="py-vh-3 bg-gray-100 border-bottom border-dark">
      <div class="container">
        <div class="row g-4 align-items-end">
          <div class="col-12 col-lg-8">
            <Breadcrumbs
              items={[
                { label: "Home", href: "/" },
                { label: "Personnel", href: pagePath, current: true },
              ]}
              className="mb-2"
            />
            <h1 class="display-4 fw-bold">Personnel</h1>
            <p class="lead text-muted mb-0">
              Profiles of law enforcement personnel with listed agencies.
            </p>
            <div class="ipc-hero-summary ipc-hero-summary-mobile mt-3">
              <span class="badge bg-light text-dark border rounded-0">
                {totalPersonnel} personnel
              </span>
              <span class="badge bg-light text-dark border rounded-0">
                {statesWithPersonnel} states covered
              </span>
            </div>
          </div>
          <div class="col-12 col-lg-4 d-none d-lg-flex justify-content-lg-end">
            <div class="ipc-hero-summary" aria-label="Personnel summary">
              <span class="badge bg-light text-dark border rounded-0">
                {totalPersonnel} personnel
              </span>
              <span class="badge bg-light text-dark border rounded-0">
                {statesWithPersonnel} states covered
              </span>
            </div>
          </div>
        </div>
      </div>
    </section>
    <section class="py-vh-2">
      <div class="container">
        <UsaChoroplethMap
          stats={stateStats}
          title="States"
          subtitle="Select a state to browse available records by place."
          emptyLabel="No personnel yet"
          interactionLabel="Hover for counts; click a state to browse available records."
          headerAction={{
            href: suggestPersonnelHref,
            label: "Suggest new person",
            target: "_self",
          }}
          federalPanel={{
            title: "Federal agencies",
            value: federalAgencies.length,
            description: "Federal agencies with listed personnel.",
            items: federalAgencies.map((agency) => ({
              label: agency.name,
              href: agency.path,
              badgeText: `${agency.branchCount} locations`,
            })),
          }}
        />
      </div>
    </section>
  </main>
</SiteLayout>
