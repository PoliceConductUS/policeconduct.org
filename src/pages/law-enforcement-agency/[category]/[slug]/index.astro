---
import { loadAgencyStaticPaths } from "#src/lib/data/agency-detail.js";

export async function getStaticPaths() {
  return loadAgencyStaticPaths();
}

// Temporary DB-backed redirect shim. Remove this route when agency legacy URL
// redirects are generated into a standalone static redirect map.
return Astro.redirect(Astro.props.canonicalAgencyPath, 301);
---
