---
import SiteLayout from "#src/layouts/SiteLayout.astro";
import Breadcrumbs from "#src/components/Breadcrumbs.astro";
import { loadAgencySummaries } from "#src/lib/data/summaries.js";
import UsaChoroplethMap from "#src/components/UsaChoroplethMap.astro";
import { US_STATE_TILES } from "#src/lib/geo/states.js";
import {
  buildItemList,
  buildWebPage,
  getSiteUrl,
} from "#src/lib/structured-data.js";

const agencies = await loadAgencySummaries();
const stateStats = US_STATE_TILES.map((state) => {
  const count = agencies.filter(
    (agency) => agency.category?.toUpperCase() === state.code,
  ).length;
  return {
    code: state.code,
    count,
    href: `/law-enforcement-agency/${state.code.toLowerCase()}/`,
  };
});
const federalAgencies = agencies.filter(
  (agency) => agency.category === "federal",
);
const totalAgencies = agencies.length;
const statesWithAgencies = stateStats.filter((state) => state.count > 0).length;
const pagePath = "/law-enforcement-agency/";
const suggestAgencyHref = "/law-enforcement-agency/new/";
const siteUrl = getSiteUrl(Astro.site);
const pageUrl = new URL(pagePath, siteUrl).toString();
const agencyItems = agencies.slice(0, 25).map((agency, index) => ({
  "@type": "ListItem",
  position: index + 1,
  item: {
    "@type": "GovernmentOrganization",
    name: agency.name,
    url: new URL(agency.canonicalPath, siteUrl).toString(),
  },
}));
const structuredData = buildWebPage({
  siteUrl,
  pageUrl,
  name: "Law Enforcement Agencies",
  description:
    "Law enforcement agencies currently listed on PoliceConduct.org.",
  type: "CollectionPage",
});
const agencyItemList = buildItemList(agencyItems);
if (agencyItemList) {
  structuredData.mainEntity = agencyItemList;
}
---

<SiteLayout
  title="Law Enforcement Agencies"
  description="Law enforcement agencies currently listed on PoliceConduct.org."
  pagePath={pagePath}
  structuredData={structuredData}
>
  <main>
    <section class="py-vh-3 bg-gray-100 border-bottom border-dark">
      <div class="container">
        <div class="row g-4 align-items-end">
          <div class="col-12 col-lg-8">
            <Breadcrumbs
              items={[
                { label: "Home", href: "/" },
                {
                  label: "Agencies",
                  href: pagePath,
                  current: true,
                },
              ]}
              className="mb-2"
            />
            <h1 class="display-4 fw-bold">Agencies</h1>
            <p class="lead text-muted mb-0">
              Law enforcement agencies tracked on PoliceConduct.org.
            </p>
            <div class="ipc-hero-summary ipc-hero-summary-mobile mt-3">
              <span class="badge bg-light text-dark border rounded-0">
                {totalAgencies} agencies
              </span>
              <span class="badge bg-light text-dark border rounded-0">
                {statesWithAgencies} states covered
              </span>
              <span class="badge bg-light text-dark border rounded-0">
                {federalAgencies.length} federal agencies
              </span>
            </div>
          </div>
          <div class="col-12 col-lg-4 d-none d-lg-flex justify-content-lg-end">
            <div class="ipc-hero-summary" aria-label="Agency summary">
              <span class="badge bg-light text-dark border rounded-0">
                {totalAgencies} agencies
              </span>
              <span class="badge bg-light text-dark border rounded-0">
                {statesWithAgencies} states covered
              </span>
              <span class="badge bg-light text-dark border rounded-0">
                {federalAgencies.length} federal agencies
              </span>
            </div>
          </div>
        </div>
      </div>
    </section>
    <section class="py-vh-2">
      <div class="container">
        <UsaChoroplethMap
          stats={stateStats}
          title="States"
          subtitle="Select a state to view law enforcement agencies in that jurisdiction."
          emptyLabel="No agencies yet"
          interactionLabel="Hover for counts; click a state to browse agencies."
          headerAction={{
            href: suggestAgencyHref,
            label: "Suggest a new agency",
            target: "_self",
          }}
          federalPanel={{
            title: "Federal agencies",
            value: federalAgencies.length,
            description: "Federal agencies listed separately from states.",
            items: federalAgencies.map((agency) => ({
              label: agency.name,
              href: agency.canonicalPath,
              badgeText: `Reports: ${agency.reportCount}`,
            })),
          }}
        />
      </div>
    </section>
  </main>
  <script
    is:inline
    id="dsq-count-scr"
    src="https://policeconduct.disqus.com/count.js"
    async></script>
</SiteLayout>
